export default function Search() {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Search</h1>
      <p>Keyword / Semantic / Hybrid search results will appear here.</p>
      <nav style={{ marginTop: '1rem' }}>
        <a href="/">Dashboard</a>
      </nav>
    </div>
  );
}
